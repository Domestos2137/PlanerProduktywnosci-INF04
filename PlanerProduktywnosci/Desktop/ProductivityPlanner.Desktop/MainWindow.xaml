<Window x:Class="ProductivityPlanner.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Planer Produktywności" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Twoje Zadania" FontSize="20" Margin="0,0,0,10"/>

        <DataGrid x:Name="dgTasks" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tytuł" Binding="{Binding Title}" Width="*"/>
                <DataGridTextColumn Header="Kategoria" Binding="{Binding Category}" Width="150"/>
                <DataGridCheckBoxColumn Header="Gotowe" Binding="{Binding IsCompleted}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>